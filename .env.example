# Database
POSTGRES_USER=retail
POSTGRES_PASSWORD=             # choose a strong password
POSTGRES_DB=retaildb

# Auth secrets — generate with: openssl rand -hex 32
AUTH_SECRET=
CUSTOMER_AUTH_SECRET=          # separate secret for customer JWTs

# App
APP_URL=https://kmz.cashbackapp.cloud
DOMAIN=kmz.cashbackapp.cloud
TZ=Asia/Yangon

# Docker image (set automatically by CI/CD)
# APP_IMAGE=ghcr.io/kaungsettshinnaing/retail-app:prod
